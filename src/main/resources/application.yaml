spring:
  sql:
    init:
      mode: always

  datasource:
    driverClassName: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3036//mydb
    username: user
    password: password

external-services:
  my-special-service:
    uri: serviceUri
    port: 6789
